<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.hui.dao.SubscribeDao">
    <insert id="addSubscribeOne" parameterType="String">
        insert into lh_subscribe(name,school,phone,major,stime) value(#{name},#{school},#{phone},#{major},NOW())
    </insert>
</mapper>